public class Main {
    public static void main(String[] args) {
        // Complex a = new Complex(1, 2);
        // Da erro pois Complex é uma interface, não pode ser instanciada

        // Complex b = new OrdinaryComplex(1, 2);
        // b.showComplex();
        // Da erro por mais que a instanci seja de OrdinaryComplex, a Interface Complex não conhece o método, e a variavel é do tipo Complex
    }
}
